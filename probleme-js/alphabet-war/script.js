const alphabetWar = (fight) => {
 let result = "";
 let leftSide = [];
 let rightSide = [];

    for (let i = 0; i < fight.lentgth; i++) {
        if (i === "w" || i === "p" || i === "b" || i === "s") {
            leftSide += 4;
        }
        else if {
            
        }
            }
        if (i === "m" || i === "q" || i === "d" || i === "z") {
            rightSide.push(fight[i]);
        }
        
    }
    
};

{
   return "Let's fight again!";
   return "Right side wins!";
   return "Let's fight again!";
}